#include <stdio.h>

/**
 * @author Alberto Ielpo <alberto.ielpo@gmail.com>
 * @brief Hello world
 *
 * This repository provides a clonable starting point for a C project.
 *
 * Compile with: "gcc -Wall -Wextra -Wpedantic -O2 -g -std=c99 -o hello hello.c" or use Makefile
 * Editor configurations for VS Code and Zed are included in the repository.
 */
int main(void) {
    printf("Hello %s\n", "World");
    return 0;
}
